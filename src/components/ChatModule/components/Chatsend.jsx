import React from "react";

const Chatsend = () => {
	return (
		<div>
			<button
				onClick={() => {
					// setState("Rahim");
				}}
			>
				change
			</button>
		</div>
	);
};

export default Chatsend;
